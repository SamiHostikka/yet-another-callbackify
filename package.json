{
  "name": "yet-another-callbackify",
  "description": "Callbackifies your promises",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project .",
    "clean": "rm -rf dist",
    "deploy": "yarn lint && yarn test-ci && yarn build",
    "lint": "tslint --project .",
    "pretest": "yarn run lint",
    "test": "jest",
    "test-ci": "yarn test -- --runInBand --coverage"
  },
  "author": "hstkk",
  "repository": "hstkk/yet-another-callbackify",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^20.0.8",
    "jest": "^20.0.4",
    "ts-jest": "^20.0.14",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  }
}